package step3

import org.junit.jupiter.api.Test

class InputViewTest {

    // private inputView = InputView()

    @Test
    fun `올바르게 입력이 들어 왔는지 테스트 합니다`() {
    }

    @Test
    fun `자동차 대수에 숫자가 아닌 다른 값을 입력 했을 떄`() {
    }

    @Test
    fun `움직일 횟수에 숫자가 아닌 다른 값을 입력 했을 때`() {
    }
}
