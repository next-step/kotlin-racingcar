package calculator

import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.params.ParameterizedTest
import org.junit.jupiter.params.provider.ValueSource

class StringCalculatorTest {

    // @ParameterizedTest
    // @ValueSource(strings = ["1,2,3", "0,0,0", "10,10,20", "4,5,9"])
    // fun `덧셈 계산`(x: Int, y: Int, expectedResult: Int) {
    //     assertThat(StringCalculator.calculate(x, y)).isEqualTo(expectedResult)
    // }
}
