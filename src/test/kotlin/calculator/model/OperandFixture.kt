package calculator.model

class OperandFixture {

    companion object {
        var one = Operand(1)
        var two = Operand(2)
        var three = Operand(3)
        var four = Operand(4)
        var five = Operand(5)
    }
}
