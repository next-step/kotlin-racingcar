class Calculator {
    fun calculation(question: String):Int{

        // 1. 숫자와 문자 구별
        // 2. 맨 처음에는 숫자가 와야함
        // 3. 숫자와 문자가 번갈아 가면서 나와야함
        // 4. 맨 나중에도 숫자가 와야함
        // 5. +,-,*,/ 이외의 문자가 올 수 없음
        // 6. / 바로 뒤에 0이 올 수 없음
        return 1
    }
}